# API name and version
api-version=@QDB_VERSION@
uberjar-name=qdb-java-api-${api-version}

# JVM args to launch tests cases
tests-argsline=-Xmx256m -Xms256m -server -XX:MaxDirectMemorySize=200m

# Path to java sources
# does not seem to work
# tests-source-dir=src/test/java
# source-dir=src/main/java

# Path to native librairies
lib-linux-dir=lib/linux
lib-bsd-dir=lib/bsd
lib-win32-dir=lib/x86
lib-win64-dir=lib/x64